import { ChainId } from '../constants/chainId';
import { TokenList } from '@NevellusSwap/token-lists';
export declare function buildList(chainId: ChainId, l1TokenList: TokenList): Promise<TokenList>;
